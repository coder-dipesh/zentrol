# ============================================================================
# Django Gesture Presentation System + Moodle LTI Integration - Requirements
# ============================================================================
# 
# Installation:
#   python3.12 -m venv venv   # or .venv — use 3.11–3.12; 3.14 may fail pinned wheels
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# Lip2Speech (/api/lip2speech/synthesize/): see section at bottom. Use the SAME venv as
# runserver. Do NOT paste the upstream Lip2Speech repo pins (torch==1.9+cu111, numpy==1.19,
# fairseq, apex, caffe2, etc.) here — they conflict with Django 4.2 and modern wheels.
# Zentrol inference uses the bundled original_model + torch 2.x stack, not the full
# legacy training notebook / YouTube / ONNX pipeline from that requirements list.
#
# ============================================================================

# ============================================================================
# Core Django Framework
# ============================================================================
Django==4.2.27
asgiref==3.11.0
sqlparse==0.5.4
pytz==2025.2
tzdata==2025.2

# ============================================================================
# Django Extensions & Plugins
# ============================================================================
# REST API framework
djangorestframework==3.14.0

# OpenAPI 3 schema generation for DRF (Swagger UI at /api/docs/)
drf-spectacular==0.27.2

# CORS support for cross-origin requests
django-cors-headers==4.3.1

# Static files serving for production (required for Vercel)
whitenoise==6.7.0

# Environment variable management
django-environ==0.11.2
python-dotenv==1.0.1

# ============================================================================
# Database Drivers
# ============================================================================
# PostgreSQL driver (optional - uncomment if using PostgreSQL)
psycopg2-binary==2.9.9

# ============================================================================
# ASGI & WebSocket Support
# ============================================================================
# ASGI server for async Django support
uvicorn==0.29.0
uvloop==0.19.0
websockets==12.0

# Django Channels for WebSocket support (optional - currently commented in asgi.py)
# channels==4.0.0
# channels-redis==4.2.0  # If using Redis as channel layer

# ============================================================================
# Image & Data Processing (if needed for future features)
# ============================================================================
# Image processing (10.2.x: facenet-pytorch requires Pillow<10.3.0,>=10.2.0)
Pillow==10.2.0
PyMuPDF==1.26.5

# Data analysis (optional - currently not directly used in code)
numpy==1.26.4
pandas==2.2.1

# ============================================================================
# Development Tools
# ============================================================================
# Code formatting
black==24.1.1
pathspec==0.12.1
mypy_extensions==1.1.0
click==8.1.8
platformdirs==4.4.0

# Code linting
flake8==7.0.0
pycodestyle==2.11.1
pyflakes==3.2.0
mccabe==0.7.0

# ============================================================================
# Testing Tools
# ============================================================================
pytest==7.4.4
pytest-django==4.8.0
iniconfig==2.1.0
pluggy==1.6.0
tomli==2.3.0

# ============================================================================
# Common Dependencies
# ============================================================================
# Standard library extensions
python-dateutil==2.9.0.post0
six==1.17.0
typing-extensions==4.15.0
exceptiongroup==1.3.1

# HTTP/Network libraries
h11==0.16.0
packaging==25.0

# ============================================================================
# Moodle / LTI 1.3 Integration
# ============================================================================
# LTI 1.3 tool provider (handles OIDC login, JWT validation, grade passback)
PyLTI1p3>=1.3.2

# RSA key generation (used by the generate_lti_keys management command)
cryptography>=42.0.0

# ============================================================================
# Algolia search (powers the dashboard ⌘K "Jump to" search)
# ============================================================================
# Optional. If ALGOLIA_APP_ID / ALGOLIA_ADMIN_API_KEY are unset in the
# environment, the dashboard search modal falls back to a static "first 6 decks"
# list and Algolia code paths are skipped entirely.
algoliasearch>=3.0,<4.0

# ============================================================================
# Lip2Speech — ML dependencies
# ============================================================================
# Deep learning framework (CPU build; swap for torch with CUDA extras for GPU)
# NOTE: facenet-pytorch pins torch ~2.2; keep these versions in sync.
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# Face recognition backbone used by Lip2Speech FaceRecognizer (pins Pillow<10.3)
facenet-pytorch>=2.6.0

# Audio processing & Griffin-Lim reconstruction
librosa>=0.10.0
soundfile>=0.12.1

# Video & image processing
opencv-python-headless>=4.9.0

# Face detection & lip landmark extraction
mediapipe>=0.10.0

# Optional — small helpers sometimes used in Lip2Speech-style repos (safe with torch 2.x)
pystoi>=0.3.3
ffmpeg-python>=0.2.0
imutils>=0.5.4
