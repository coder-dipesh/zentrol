# Slim dependencies for Vercel / serverless — stay under the ~245 MB Python bundle limit.
# Omits Lip2Speech ML stack (torch, mediapipe python pkg, opencv, librosa, …), PyMuPDF,
# ASGI servers, and dev/test tools — see requirements.txt for full local development.
#
# Install: pip install -r requirements-vercel.txt
# Vercel: vercel.json sets installCommand to this file when deploying.

Django==4.2.27
asgiref==3.11.0
sqlparse==0.5.4
pytz==2025.2
tzdata==2025.2

djangorestframework==3.14.0
drf-spectacular==0.27.2
django-cors-headers==4.3.1
whitenoise==6.7.0

django-environ==0.11.2
python-dotenv==1.0.1

psycopg2-binary==2.9.9

Pillow==10.2.0

PyLTI1p3>=1.3.2
cryptography>=42.0.0

algoliasearch>=3.0,<4.0

# Common transitive pins (speeds resolves; safe with Django 4.2 stack above)
typing-extensions==4.15.0
packaging==25.0
python-dateutil==2.9.0.post0
six==1.17.0
exceptiongroup==1.3.1
