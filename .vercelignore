# Shrink upload/build context for Vercel (source still in git for non-Vercel deploys).
lip2speech/
docs/
docker/
scripts/
docker-compose.yml
docker-compose.moodle.yml
download_mediapipe.sh
build.sh
.git/
.pytest_cache/
htmlcov/
# Same rule as .gitignore: anchor so ``static/media/`` ships with the bundle.
/media/
db.sqlite3

# Demo-only assets (~22 MB) — keep in git for VM/docker deploys; omit from serverless bundle.
# Server templates use IS_SERVERLESS to skip hero video; presentation thumbnails may 404 on Vercel.
static/media/slides/
static/media/video/
